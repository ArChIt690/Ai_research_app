# ── Web Search ────────────────────────────────────────────────────────────────
tavily-python==0.5.0             # Tavily Search API client (low-level)
langchain-tavily==0.2.18         # Official LangChain Tavily integration (Search/Extract tools)

# ── Web Scraping ──────────────────────────────────────────────────────────────
beautifulsoup4==4.12.3           # HTML parsing & scraping
requests==2.32.5                 # HTTP requests for fetching web pages (>=2.32.5 required by langchain-community)
lxml==5.2.2                      # Fast XML/HTML parser (BS4 backend)
httpx==0.27.0                    # Async HTTP client for async scraping

# ── LangChain Core & LCEL ─────────────────────────────────────────────────────
langchain==1.3.10                # Core LCEL | RunnableSequence, RunnableParallel, RunnableLambda etc.
langchain-core==1.4.7            # Base abstractions: Runnables, prompts, output parsers
langchain-community==0.4.2       # Community integrations (loaders, misc tools)

# ── ReAct Agent (LangGraph) ───────────────────────────────────────────────────
langgraph==1.2.6                 # StateGraph, tool nodes, checkpointing, create_agent runtime

# ── Mistral AI via LangChain ──────────────────────────────────────────────────
langchain-mistralai==1.1.5       # ChatMistralAI LLM wrapper (plugs into LCEL chains natively)
mistralai==1.2.5                 # Official Mistral SDK (needs pydantic>=2.9.0)

# ── Environment & Config ──────────────────────────────────────────────────────
python-dotenv==1.0.1             # Load API keys from .env file

# ── Data Validation ───────────────────────────────────────────────────────────
pydantic==2.11.7                 # Structured output schemas for critic scores/feedback

# ── Utilities ─────────────────────────────────────────────────────────────────
tenacity==8.3.0                  # Retry logic for flaky API/scrape calls
tqdm==4.66.4                     # Progress bars for multi-URL scraping loops
rich==13.7.1                     # Pretty terminal output for agent reasoning traces